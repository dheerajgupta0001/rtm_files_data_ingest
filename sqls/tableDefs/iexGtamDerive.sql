CREATE TABLE MO_WAREHOUSE.IEX_GTAM_DERIVE (
	ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
	TIME_STAMP DATE,
    CONTRACT_TYPE VARCHAR2(250),
    highest_price NUMBER(25,4),
    lowest_price NUMBER(25,4),
    max_trades NUMBER(25,4),
    total_trades NUMBER(25,4),
    total_traded_vol NUMBER(25,4),
    UNIQUE(time_stamp,CONTRACT_TYPE)
);