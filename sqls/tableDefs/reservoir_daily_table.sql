CREATE TABLE MIS_WAREHOUSE.RESERVOIR_DAILY_DATA (
	ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
	TIME_STAMP DATE NOT NULL,
	ENTITY_TAG VARCHAR2(100) NOT NULL,
	METRIC_TAG VARCHAR2(100) NOT NULL,
	DATA_VALUE NUMBER(38,0),
	UNIQUE(ENTITY_TAG,METRIC_TAG,TIME_STAMP)
);