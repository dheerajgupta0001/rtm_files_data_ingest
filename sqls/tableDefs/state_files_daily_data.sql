CREATE TABLE MIS_WAREHOUSE.STATE_FILES_DAILY_DATA (
	ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
	TIME_STAMP DATE NOT NULL,
	ENTITY_TAG VARCHAR2(100) NOT NULL,
	METRIC_NAME VARCHAR2(100) NOT NULL,
	DATA_VALUE NUMBER(25,4),
	UNIQUE(ENTITY_TAG,METRIC_NAME,TIME_STAMP)
);
